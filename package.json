{
  "name": "front-end-challenge-vagas.com",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "npx eslint *.js ",
    "eslint": "npx stylelint *.css",
    "deploy": "npm run build && vercel --prod",
    "test": "cypress run",
    "cypress:open": "cypress open"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "cypress": "^13.7.3",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "stylelint": "^16.3.1",
    "stylelint-config-standard": "^36.0.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "git-commit-msg-linter": "^5.0.7"
  }
}
